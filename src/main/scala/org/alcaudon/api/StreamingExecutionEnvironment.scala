package alcaudon.api

import alcaudon.core._

class StreamingExecutionEnvironment {
  // def addSource[T: TypeInfo](src: SourceFn[T]): DataStream[T] = {
  //   val typeInfo = implicitly[TypeInfo[T]]
  //   println(typeInfo.name)
  // }
}
